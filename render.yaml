services:
  - type: web
    name: employeesuite
    env: python
    region: oregon
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --worker-class=sync --workers=1 --timeout=120 main:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: SHOPIFY_API_KEY
        sync: false
      - key: SHOPIFY_API_SECRET
        sync: false
      - key: SHOPIFY_APP_URL
        fromService:
          type: web
          name: employeesuite
          property: url
      - key: SCOPES
        value: read_orders,read_products,read_inventory
      - key: SECRET_KEY
        generateValue: true
      - key: ENCRYPTION_KEY
        generateValue: true
      - key: ENVIRONMENT
        value: production
      - key: FLASK_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: employeesuite-db
          property: connectionString
    autoDeploy: true

databases:
  - name: employeesuite-db
    region: oregon
    plan: starter
    databaseName: employeesuite
    user: employeesuite_user
