[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    --tb=short
    --strict-markers
    --disable-warnings
markers =
    smoke: Critical smoke tests that must pass before deployment
    integration: Integration tests
    unit: Unit tests
